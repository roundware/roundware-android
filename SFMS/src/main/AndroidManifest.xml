<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.si.sfms"
    android:versionCode="1"
    android:versionName="1.0" >

    <!--
        Currently have to use API 9 level to avoid StrictMode exceptions on http requests
        happening on main thread in RWFramework - requires some rewrite to fix it
    -->
    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="9" />

    <!-- Required user permissions -->
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <uses-feature
            android:glEsVersion="0x00020000"
            android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <service
                android:name="com.halseyburgund.rwframework.core.RWService"
                android:enabled="true"/>

        <!-- API Key for debug -->
        <!-- AIzaSyCqCmjrNLtEV-g9SUrBFLjOf3OkkNO25mk -->
        <!-- API Key for release -->
        <!-- ***REMOVED*** -->
        <meta-data
                android:name="com.google.android.maps.v2.API_KEY"
                android:value="***REMOVED***"/>

        <activity
            android:name="edu.si.sfms.SFMSMainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:clearTaskOnLaunch="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="edu.si.sfms.SFMSListenActivity"
            android:label="@string/title_activity_sfmslisten"
            android:launchMode="singleTask"
            android:clearTaskOnLaunch="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name="edu.si.sfms.SFMSSpeakActivity"
            android:label="@string/title_activity_sfmsspeak"
            android:launchMode="singleTask"
            android:clearTaskOnLaunch="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name="edu.si.sfms.SFMSPrefsActivity"
            android:label="@string/title_activity_sfmsprefs" >
        </activity>
    </application>

</manifest>
